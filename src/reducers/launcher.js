
const initialState = {
  selectedForm: null,
  formName: null,
};

const launcher = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default launcher;
