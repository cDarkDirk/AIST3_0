import {
        SCHEDULE_DATE_CHANGED
} from '../constants'

const initialState = null

const sheduleForm = (state = initialState, action) => {
               return state

}

export default sheduleFormReducer
